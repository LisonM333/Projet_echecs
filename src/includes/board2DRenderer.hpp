// #pragma once
// #include "board.hpp"

// const char* get_label(const int& line, const int& colum);

// template<typename T, typename Q>
// // bool is_in(const T& value, const std::vector<Q>& list); // T and Q are different only for the case of the comparaison between char* and string

// bool square_colored(const std::vector<Position>& squares, const Position& square);
// void square_representation(const int& line, const int& colum, const char*& label, const ImVec4& colo_case, const ImVec4& colo_piece, bool& selected); // responsabilise the apparence of one square

// void board2DRendererImGui(Board const& board);